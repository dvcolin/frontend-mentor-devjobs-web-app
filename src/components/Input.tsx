import styled from "styled-components";

const Input = styled.input`
  font-family: inherit;
  font-size: inherit;
  padding: 0.8rem;
  border: none;
  &:focus-visible {
    outline: none;
  }
`;

export default Input;
